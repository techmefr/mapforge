# Storage

Layer technique de persistance. Fournit un composable generique pour
sauvegarder/charger un etat serialisable en JSON dans le `localStorage`
du navigateur, avec debounce et gestion d'erreur silencieuse.

Reutilisable dans n'importe quel projet Nuxt, aucun lien avec le metier.
